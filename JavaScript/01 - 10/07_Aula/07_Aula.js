const nome = 'Gabriel'; // String
const num = 22; // Number
const sobreNomeAluno = null; // Null -> não aponta para local nenhum na memória explicitamente
const aprovado = true; // Boolean -> true, false (lógico)

let nomeAluno; // Undefined -> não aponta para local nenhum na memória 

console.log(typeof(nome));
console.log(typeof(num));
console.log(typeof(sobreNomeAluno));
console.log(typeof(aprovado));
console.log(typeof(nomeAluno));