let num1 = window.prompt('Digite um número');
let num2 = window.prompt('Digite outro número');

const resultado = Number(num1) + Number(num2);

window.alert(`O resultado da soma de ${num1} e ${num2} é: ${resultado}`);